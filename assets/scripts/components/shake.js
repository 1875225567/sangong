cc.Class({
    extends: cc.Component,
  
    properties: {
          
    },

    // use this for initialization
    onLoad: function () {
        // var name = "armatureName";
        // var shake = this.getComponent(dragonBones.ArmatureDisplay);
        // shake.armatureName = name;
        // shake.playAnimation(); 
    },
});